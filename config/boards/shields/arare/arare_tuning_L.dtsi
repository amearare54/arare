/* arare チューニング（左手）: 左エンコーダ＋ジョイスティック（受け口）
 *
 * このファイルは arare Studio の「設定」パネルが生成するテキストでまるごと差し替える運用。
 * 既定値 = 現状維持。
 */

/* 左エンコーダ（EC12・縦）の分解能と感度 */
&left_encoder {
    steps = <12>;
};

&sensors {
    triggers-per-rotation = <10>;
};

/* ---- ジョイスティック（アナログXY）: フェーズ2で有効化予定 ----
 * ZMK本体のバッテリー計測とSAADC設定が衝突する既知問題があるため未実装。
 * arare Studio のジョイスティック設定はこのコメントブロックとして書き出され、
 * 実装後にそのまま有効化できる形にしてある。
 * (設定なし)
 * ------------------------------------------------------------- */

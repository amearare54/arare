/* 切り分け用（left-solo-test）: 左手を単独動作させる。
 * ジョイスティックは zmk,input-split 経由なので、split を切るこのビルドでは無効にする。 */

&left_encoder {
    steps = <12>;
};

&sensors {
    triggers-per-rotation = <10>;
};

&joystick {
    status = "disabled";
};

&joystick_split {
    status = "disabled";
};
